# auto-swap-menu
When the user visits the website for the first time in a session, the "Reference" and "Community" menu items must swap places.
The swap happen only once per session and remain swapped until the session ends.
Only the "Reference" and "Community" menu items is being swapped — no other elements are affected.
The swap is handle safely without causing any UI issues or layout breaks across the website.

